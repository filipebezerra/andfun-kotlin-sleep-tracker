<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/apk/res-auto"
    >
    <data>
        <variable
            name="sleepNight"
            type="dev.filipebezerra.android.sleeptracker.database.SleepNight"
            />
        <variable
            name="clickListener"
            type="dev.filipebezerra.android.sleeptracker.sleeptracker.SleepNightListener"
            />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/normal_spacing"
        android:onClick="@{() -> clickListener.onClick(sleepNight)}"
        >

        <ImageView
            android:id="@+id/sleep_quality_image"
            android:layout_width="@dimen/icon_size"
            android:layout_height="@dimen/icon_size"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            bind:sleepQualityImage="@{sleepNight.sleepQuality}"
            bind:sleepQualityDescription="@{sleepNight.startTimeMillis}"
            tools:srcCompat="@drawable/ic_sleep_0"
            tools:ignore="ContentDescription"
            />

        <TextView
            android:id="@+id/sleep_quality_text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/small_spacing"
            android:textAppearance="?attr/textAppearanceBody1"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="@+id/sleep_quality_image"
            app:layout_constraintTop_toBottomOf="@+id/sleep_quality_image"
            bind:sleepQualityText="@{sleepNight.sleepQuality}"
            tools:text="Excellent!!!"
            />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>